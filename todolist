addtolist(

update analog read property{

find instance number;

get head object[index number]
goto not_pv;?
analog present value set(instance, data),

}


to setup random data pool go into random_data
ex. 2 instances device number 26.
mkdir 26
cd 26
dd if=/dev/urandom of=0 bs=2 count=27
dd if=/dev/urandom of=1 bs=2 count=52

hexdump 0
hexdump 1


bits to change:
implement array based link list system (hello_world.c)
update analog read property function (reading from list)
insert a function that runs as seperate thread and sleeps every 100ms and contacts modbus server.***
